---
phase: 07-replace-the-ui-library-used-by-the-vst-wrapper-with-the-modern-standard-for-cross-platform-vst3-development
plan: 02
type: execute
wave: 2
depends_on: [07-01]
files_modified:
  - crates/agentaudio-wrapper-vst3/src/lib.rs
autonomous: true
requirements: [HOST-02, MCP-01, GUI-03]
must_haves:
  truths:
    - "UI matches previous functional layout"
    - "Textbox updates GuiState"
    - "Buttons trigger SharedState actions"
    - "ResizeHandle is positioned correctly"
  artifacts:
    - path: "crates/agentaudio-wrapper-vst3/src/lib.rs"
      contains: "VStack::new"
    - path: "crates/agentaudio-wrapper-vst3/src/lib.rs"
      contains: "Textbox::new"
    - path: "crates/agentaudio-wrapper-vst3/src/lib.rs"
      contains: "ResizeHandle::new"
  key_links:
    - from: "Textbox"
    - to: "GuiState.plugin_path"
    - via: "Lens mapping"
---

<objective>
Implement the full Vizia UI layout for the VST3 wrapper, replicating the functionality of the previous egui interface.
Purpose: Restore user controls for plugin loading, editor management, and status monitoring using the new Vizia framework.
Output: Compilable code with a complete, functional Vizia GUI.
</objective>

<execution_context>
@./.gemini/get-shit-done/workflows/execute-plan.md
@./.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-replace-the-ui-library-used-by-the-vst-wrapper-with-the-modern-standard-for-cross-platform-vst3-development/07-RESEARCH.md
@crates/agentaudio-wrapper-vst3/src/lib.rs
</context>

<tasks>

<task type="auto">
  <name>Implement Vizia UI Layout</name>
  <files>crates/agentaudio-wrapper-vst3/src/lib.rs</files>
  <action>
    Flesh out the `editor()` function's `create_vizia_editor` closure:
    
    1. Use `VStack` as main container.
    2. Add `Label`s for Instance ID, MCP Name, Endpoint (bound via `EditorData::shared` lens).
    3. Add `Textbox` for `plugin_path` (bound via `EditorData::gui_state` lens mapping).
       - Use `on_edit` or `on_submit` to update `GuiState` (lock mutex).
    4. Add `HStack` for Load/Unload buttons.
       - Load: call `shared.load_child_plugin_by_path`.
       - Unload: call `shared.unload_child_plugin`.
    5. Add `HStack` for Open/Close Editor buttons.
       - Open: call `shared.open_editor`.
       - Close: call `shared.close_editor`.
    6. Add `Label`s for Loaded info, Message, Queue size.
    7. Add `ResizeHandle::new(cx)` as the LAST element.
    
    Use `EditorData` lens for all bindings. Ensure Mutex locking is handled correctly in closures (not blocking main thread, though `shared` methods might block briefly, which is acceptable for this wrapper UI).
  </action>
  <verify>cargo build -p agentaudio-wrapper-vst3</verify>
  <done>UI code is fully implemented and compiles</done>
</task>

<task type="auto">
  <name>Verify Functionality</name>
  <files>crates/agentaudio-wrapper-vst3/src/lib.rs</files>
  <action>
    Run existing tests to ensure no regression in logic.
    Since UI is visual, compilation and correct Lens usage is the primary automated check.
  </action>
  <verify>cargo test -p agentaudio-wrapper-vst3</verify>
  <done>Tests pass</done>
</task>

</tasks>

<verification>
Run `cargo build` and `cargo test`.
</verification>

<success_criteria>
- Build passes
- Tests pass
- Code uses Vizia widgets correctly (ResizeHandle last, Lens bindings)
</success_criteria>

<output>
After completion, create `.planning/phases/07-replace-the-ui-library-used-by-the-vst-wrapper-with-the-modern-standard-for-cross-platform-vst3-development/07-02-SUMMARY.md`
</output>
