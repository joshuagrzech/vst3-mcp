---
phase: 07-replace-the-ui-library-used-by-the-vst-wrapper-with-the-modern-standard-for-cross-platform-vst3-development
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/agentaudio-wrapper-vst3/Cargo.toml
  - crates/agentaudio-wrapper-vst3/src/lib.rs
autonomous: true
requirements: [HOST-02, MCP-01, GUI-03]
must_haves:
  truths:
    - "Project compiles with nih_plug_vizia"
    - "nih_plug_egui is removed from dependencies"
    - "WrapperParams uses ViziaState"
    - "editor() uses create_vizia_editor"
  artifacts:
    - path: "crates/agentaudio-wrapper-vst3/Cargo.toml"
      contains: "nih_plug_vizia"
    - path: "crates/agentaudio-wrapper-vst3/src/lib.rs"
      contains: "create_vizia_editor"
  key_links:
    - from: "WrapperParams.editor_state"
      to: "ViziaState"
      via: "Arc<ViziaState>"
---

<objective>
Migrate the VST3 wrapper from nih_plug_egui to nih_plug_vizia by updating dependencies and establishing the Vizia-compatible data structures.
Purpose: Replace the soft-deprecated egui interface with the modern standard for nih-plug (vizia) to ensure long-term stability and better widget support.
Output: Compilable code with Vizia dependencies and a minimal Vizia editor initialization.
</objective>

<execution_context>
@./.gemini/get-shit-done/workflows/execute-plan.md
@./.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-replace-the-ui-library-used-by-the-vst-wrapper-with-the-modern-standard-for-cross-platform-vst3-development/07-RESEARCH.md
@crates/agentaudio-wrapper-vst3/Cargo.toml
@crates/agentaudio-wrapper-vst3/src/lib.rs
</context>

<tasks>

<task type="auto">
  <name>Swap Dependencies</name>
  <files>crates/agentaudio-wrapper-vst3/Cargo.toml</files>
  <action>
    Remove `nih_plug_egui` dependency and add `nih_plug_vizia`.
    
    ```toml
    # Remove
    nih_plug_egui = { git = "https://github.com/robbert-vdh/nih-plug.git" }
    
    # Add
    nih_plug_vizia = { git = "https://github.com/robbert-vdh/nih-plug.git" }
    ```
    Ensure other dependencies remain unchanged.
  </action>
  <verify>grep "nih_plug_vizia" crates/agentaudio-wrapper-vst3/Cargo.toml</verify>
  <done>Cargo.toml has nih_plug_vizia and not nih_plug_egui</done>
</task>

<task type="auto">
  <name>Update Data Structures & Editor Init</name>
  <files>crates/agentaudio-wrapper-vst3/src/lib.rs</files>
  <action>
    1. Update imports: remove `nih_plug_egui` items, add `nih_plug_vizia::{ViziaState, ViziaTheming, create_vizia_editor, vizia::prelude::*}`.
    2. Update `WrapperParams`: change `editor_state` type to `Arc<ViziaState>` and `default()` to use `ViziaState::from_size(560, 420)`.
    3. Add `#[derive(Clone)]` to `GuiState` (it already has Default).
    4. Define `EditorData` struct:
       ```rust
       #[derive(Lens, Clone)]
       struct EditorData {
           shared: SharedState,
           gui_state: Arc<Mutex<GuiState>>,
       }
       impl Model for EditorData {}
       ```
    5. Replace `editor()` implementation:
       - Construct `EditorData`.
       - Call `create_vizia_editor` with `ViziaTheming::Custom`.
       - In the build closure, call `data.build(cx)`.
       - Create a minimal UI: `VStack::new(cx, |cx| { Label::new(cx, "Migration in progress..."); });`.
    6. Ensure `SharedState` is treated correctly (it's already Clone).
  </action>
  <verify>cargo build -p agentaudio-wrapper-vst3</verify>
  <done>Code compiles with Vizia types and minimal editor</done>
</task>

</tasks>

<verification>
Run `cargo build -p agentaudio-wrapper-vst3` to ensure no type errors with the new library.
</verification>

<success_criteria>
- Cargo.toml uses nih_plug_vizia
- lib.rs compiles without errors
- WrapperParams uses ViziaState
</success_criteria>

<output>
After completion, create `.planning/phases/07-replace-the-ui-library-used-by-the-vst-wrapper-with-the-modern-standard-for-cross-platform-vst3-development/07-01-SUMMARY.md`
</output>
