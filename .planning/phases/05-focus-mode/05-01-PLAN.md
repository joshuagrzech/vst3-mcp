---
phase: 05-focus-mode
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/hosting/types.rs
  - src/server.rs
  - src/bin/focus_mode_test.rs
autonomous: true

must_haves:
  truths:
    - "With a Focus Mode JSON config specifying 3 exposed parameters, list_params returns exactly those 3 parameters"
    - "A config file maps plugin classId to a set of exposed parameter IDs, so different plugins have independent Focus Mode settings"
    - "Without any config file, list_params returns all non-read-only parameters (sensible default)"
    - "Modifying the Focus Mode config and calling reload_focus_config changes which parameters appear in list_params"
  artifacts:
    - path: "src/hosting/types.rs"
      provides: "FocusConfig struct with HashMap<String, HashSet<u32>> for classId->paramID mapping"
      exports: ["FocusConfig", "FocusConfig::load_from_file", "FocusConfig::get_exposed_params"]
    - path: "src/server.rs"
      provides: "Focus Mode filtering in list_params and reload_focus_config tool"
      contains: "focus_config: Arc<Mutex<Option<FocusConfig>>>"
    - path: "src/bin/focus_mode_test.rs"
      provides: "Integration test validating all 4 Phase 5 success criteria"
      min_lines: 300
  key_links:
    - from: "src/server.rs::AudioHost"
      to: "src/hosting/types.rs::FocusConfig"
      via: "Arc<Mutex<Option<FocusConfig>>> field"
      pattern: "focus_config.*FocusConfig"
    - from: "src/server.rs::list_params"
      to: "FocusConfig::get_exposed_params"
      via: "HashSet membership filtering"
      pattern: "get_exposed_params.*contains"
    - from: "src/server.rs::reload_focus_config"
      to: "FocusConfig::load_from_file"
      via: "JSON reload and mutex update"
      pattern: "load_from_file.*focus_config.*lock"
---

<objective>
Implement Focus Mode parameter filtering so AI only sees user-selected VST3 parameters, reducing context noise from hundreds of irrelevant controls.

Purpose: Large plugins (synths, complex effects) expose 100+ parameters. Most are irrelevant for typical AI-assisted workflows. Focus Mode lets users whitelist only the parameters they care about (e.g., 3-5 key controls), making list_params output concise and Claude's context usage efficient.

Output: FocusConfig struct, filtered list_params, reload_focus_config MCP tool, and integration tests validating all 4 success criteria.
</objective>

<execution_context>
@/home/obscurorevelio/.claude/get-shit-done/workflows/execute-plan.md
@/home/obscurorevelio/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-focus-mode/05-RESEARCH.md
@.planning/phases/04-mcp-server-tools/04-01-SUMMARY.md

# Existing codebase context
@src/hosting/types.rs  # Add FocusConfig here (next to PluginInfo)
@src/server.rs          # Extend AudioHost with focus_config field, modify list_params
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create FocusConfig struct with JSON loading and case-insensitive lookup</name>
  <files>src/hosting/types.rs</files>
  <action>
Add FocusConfig struct to src/hosting/types.rs following the research pattern:

1. **FocusConfig struct** with serde Deserialize/Serialize:
   - Field: `focus_mode: HashMap<String, HashSet<u32>>` (plugin classId -> exposed param IDs)
   - Derive: Debug, Clone, Deserialize, Serialize

2. **FocusConfig::load_from_file(path: &str) -> Result<Self, String>**:
   - Read file with std::fs::read_to_string
   - Parse JSON with serde_json::from_str
   - Error handling: Return descriptive errors for missing file (NotFound) vs parse errors (line/column)
   - Pattern from research: format!("Invalid JSON in '{}': {} (line {}, column {})", path, e, e.line(), e.column())

3. **FocusConfig::get_exposed_params(&self, class_id: &str) -> Option<&HashSet<u32>>**:
   - Normalize class_id to uppercase for case-insensitive lookup
   - Use self.focus_mode.get(&normalized_key)
   - Returns None if plugin classId not in config (falls back to "expose all")

4. **Add necessary imports**:
   - use std::collections::{HashMap, HashSet};
   - serde already imported in types.rs

Follow existing code style in types.rs (doc comments on public items, same formatting).

JSON schema expected:
```json
{
  "focus_mode": {
    "PLUGIN_CLASSID_HEX": [1, 5, 12],
    "ANOTHER_CLASSID_HEX": [0, 3, 7]
  }
}
```

Do NOT add version fields or extra complexity - YAGNI per research.
  </action>
  <verify>
cargo build --release --lib passes without warnings.
grep -A5 "pub struct FocusConfig" src/hosting/types.rs shows HashMap field.
grep "pub fn load_from_file" src/hosting/types.rs confirms method exists.
  </verify>
  <done>
FocusConfig struct exists in src/hosting/types.rs with load_from_file and get_exposed_params methods. Compiles cleanly.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add Focus Mode filtering to AudioHost and list_params</name>
  <files>src/server.rs</files>
  <action>
Modify src/server.rs to integrate Focus Mode configuration:

1. **Import FocusConfig**:
   - Add to existing use statement: `use vst3_mcp_host::hosting::types::{PluginInfo, FocusConfig};`
   - Add std imports: `use std::collections::HashSet;`

2. **Add focus_config field to AudioHost struct**:
   - Field: `focus_config: Arc<Mutex<Option<FocusConfig>>>`
   - Place after scan_cache field, before tool_router

3. **Initialize focus_config in AudioHost::new()**:
   - Try to load from "focus_mode.json" (project-local path per research recommendation)
   - If file exists and loads successfully: Some(config) with info! log showing plugin count
   - If file missing or invalid: None with info! log explaining "exposing all writable params"
   - Pattern: `FocusConfig::load_from_file("focus_mode.json").ok()` for graceful fallback
   - Initialize field: `focus_config: Arc::new(Mutex::new(focus_config))`

4. **Modify list_params tool to add Focus Mode filtering**:
   - After locking plugin, also lock plugin_info to get current plugin classId
   - Lock focus_config to get exposed parameter set
   - Use match on (config, class_id) to get exposed HashSet (None = expose all)
   - Add third filter in parameter enumeration loop AFTER existing is_writable() && !is_hidden():
     ```rust
     // Filter 2: Must be in Focus Mode config (if config exists)
     if let Some(allowed) = exposed_params {
         if !allowed.contains(&info.id) {
             continue; // Parameter not exposed in Focus Mode
         }
     }
     ```
   - Keep existing filters - Focus Mode is ADDITIONAL, not replacement (prevents read-only param exposure)
   - Update info! log at end: "list_params found {} exposed parameters (Focus Mode: {})", count, is_filtered

5. **Add reload_focus_config MCP tool**:
   - Request struct: `ReloadFocusConfigRequest { path: Option<String> }` with schema description
   - Tool description: "Reload Focus Mode configuration from JSON file. Changes apply to next list_params call."
   - Parameters: path defaults to "focus_mode.json" if None
   - Logic: Load config with FocusConfig::load_from_file, return error if fails, update Arc<Mutex<>>
   - Response JSON: {"status": "reloaded", "path": path, "plugin_count": config.focus_mode.len()}

Follow exact lock ordering to avoid deadlock: plugin -> plugin_info -> focus_config (research Pitfall 4).

Do NOT use lazy_static or global state - use Arc<Mutex<Option<>>> for reloadability per research.
  </action>
  <verify>
cargo build --release passes without warnings.
grep "focus_config: Arc<Mutex<Option<FocusConfig>>>" src/server.rs confirms field added.
grep -A10 "fn list_params" src/server.rs | grep "exposed_params" confirms filtering logic.
grep "#\[tool.*reload_focus_config" src/server.rs confirms new tool exists.
  </verify>
  <done>
AudioHost has focus_config field, list_params filters by Focus Mode when config exists, reload_focus_config tool allows runtime config updates. All compiles without warnings.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create integration test validating all 4 Phase 5 success criteria</name>
  <files>src/bin/focus_mode_test.rs</files>
  <action>
Create new integration test binary following the pattern from mcp_integration_test.rs (Phase 4):

1. **Binary setup** (Cargo.toml already configured for bin/* pattern):
   - Name: focus_mode_test
   - Imports: Same as mcp_integration_test (std::process, std::io, serde_json, etc.)
   - MCP handshake helper: Reuse from Phase 4 pattern

2. **Test 1: With Focus Mode config, list_params returns only exposed parameters**:
   - Create test config: focus_mode_test_config.json with plugin classId -> [param_id_1, param_id_2, param_id_3]
   - Load plugin via MCP
   - Call reload_focus_config with test config path
   - Call list_params, verify count == 3 and ids match config exactly
   - Clean up test config file

3. **Test 2: Different plugins have independent Focus Mode settings**:
   - Test config maps TWO different classIds to different param sets
   - Load first plugin, verify list_params returns first param set
   - (If second plugin available) load second plugin, verify different params
   - Otherwise validate that config structure supports multiple classIds (inspect JSON)

4. **Test 3: Without config file, list_params returns all writable parameters**:
   - Delete config file or use non-existent path
   - Load plugin
   - Call list_params, verify count > 0 (all writable params exposed)
   - Verify count matches original list_params (no filtering applied)

5. **Test 4: reload_focus_config changes which parameters appear**:
   - Load plugin, call list_params (baseline count)
   - Create new config with subset of parameters
   - Call reload_focus_config
   - Call list_params again, verify count decreased and only config params present
   - Clean up

**Graceful skip pattern**: If no plugin found (PLUGIN_PATH env var not set), skip all tests with informative message like Phase 2/3/4 tests.

**Test structure**: PASS/FAIL reporting like Phase 1-4 integration tests. Print summary at end.

**Error handling**: Check MCP response isError flag, extract error messages from content array.

Use AGain plugin as default test target (consistent with Phase 4). If AGain has fewer than 3 writable params, adjust test to use min(available, 3) like Phase 4 batch_set test.
  </action>
  <verify>
cargo build --release --bin focus_mode_test succeeds.
PLUGIN_PATH=/path/to/plugin.vst3 cargo run --release --bin focus_mode_test shows 4/4 PASSED.
Test creates and cleans up temporary config files.
Test handles missing plugin gracefully with skip message.
  </verify>
  <done>
Integration test binary exists, validates all 4 Phase 5 success criteria with real VST3 plugin. Tests pass and clean up temporary files. Graceful skip when plugin unavailable.
  </done>
</task>

</tasks>

<verification>
After all tasks complete:

1. **Compile check**: `cargo build --release` passes with no warnings
2. **Unit tests**: `cargo test --lib` passes (existing tests unaffected)
3. **Integration test**: `PLUGIN_PATH=/path/to/plugin.vst3 cargo run --release --bin focus_mode_test` shows 4/4 PASSED
4. **Manual MCP test**:
   - Create focus_mode.json with real plugin classId and 2-3 param IDs
   - Start MCP server, call load_plugin
   - Call list_params, verify only configured params returned
   - Modify focus_mode.json (change param IDs)
   - Call reload_focus_config
   - Call list_params again, verify different params returned
5. **File existence**:
   - src/hosting/types.rs contains FocusConfig struct
   - src/server.rs contains focus_config field and reload_focus_config tool
   - src/bin/focus_mode_test.rs exists and is executable
</verification>

<success_criteria>
Phase 5 Focus Mode complete when:

1. ✅ **Config-based filtering**: With focus_mode.json specifying 3 parameter IDs for a plugin, list_params returns exactly those 3 parameters (not all 100+)
2. ✅ **Per-plugin configuration**: Config file maps multiple plugin classIds to different parameter sets, demonstrating independent Focus Mode per plugin
3. ✅ **Sensible default**: Without focus_mode.json or when plugin classId not in config, list_params returns all writable parameters (existing Phase 4 behavior preserved)
4. ✅ **Runtime reload**: Calling reload_focus_config after modifying focus_mode.json updates list_params output without restarting the MCP server

Integration test validates all 4 success criteria with real VST3 plugin.
Code follows research patterns: HashMap<String, HashSet<u32>>, Arc<Mutex<Option<>>>, case-insensitive classId lookup, O(1) HashSet filtering.
</success_criteria>

<output>
After completion, create `.planning/phases/05-focus-mode/05-01-SUMMARY.md` following the Phase 4 summary template:
- What was built (FocusConfig, filtering logic, reload tool, integration test)
- Deviations from plan (if any auto-fixed issues encountered)
- Test results (integration test output, success criteria validation)
- Technical details (JSON schema, filtering pattern, lock ordering)
- Files modified (created + modified)
- Commits (feat: add FocusConfig, feat: add Focus Mode filtering, test: add integration test)
</output>
